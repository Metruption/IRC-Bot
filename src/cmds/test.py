def test(components):
    response = ''
    print('now inside the test command fucking fucking this is the arguments shitfucking fuck \''+components['arguments']+'\'preious needs to fuck its bitch ass nigger butts')

    if components['arguments'] == '!test':
        # the user sent just the command, no garbage
        response = 'test successful'
        print('just responded to test successful!')
    return response